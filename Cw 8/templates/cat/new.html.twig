{% extends 'base.html.twig' %}

{% block title %}New Cat{% endblock %}

{% block body %}
    <h1>Create new Cat</h1>
    <div class="low-button">
        <a href="{{ path('app_cat_index') }}" class="btn btn-secondary btn-lg btn-block">back to
            list</a>
    </div>
    <div class="small-btn">{{ include('cat/_form.html.twig') }}</div>



{% endblock %}
